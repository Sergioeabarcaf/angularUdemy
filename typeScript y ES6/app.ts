let nombre:string = "Juan"
let apellido:string = "Perez"
let edad:number = 30

//let texto = "Hola" + nombre + " " + apellido + " ("+edad+")"
let texto = `hola, ${nombre} ${apellido} (${edad})`;

console.log(texto);
